import { PCA } from './pca';
export interface SparsePCAProps {
    nComponents?: number | null;
    alpha?: number;
    maxIter?: number;
    tol?: number;
}
export declare class SparsePCA extends PCA {
    private alpha;
    private maxIter;
    private tol;
    constructor(props?: SparsePCAProps);
    fit(X: number[][]): void;
}

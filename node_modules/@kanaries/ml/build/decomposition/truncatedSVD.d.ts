export declare class TruncatedSVD {
    private nComponents;
    private components;
    private singularValues;
    private explainedVariance;
    private explainedVarianceRatio;
    constructor(nComponents?: number);
    fit(X: number[][]): void;
    transform(X: number[][]): number[][];
    fitTransform(X: number[][]): number[][];
    inverseTransform(X: number[][]): number[][];
    getComponents(): number[][];
    getSingularValues(): number[];
    getExplainedVariance(): number[];
    getExplainedVarianceRatio(): number[];
}

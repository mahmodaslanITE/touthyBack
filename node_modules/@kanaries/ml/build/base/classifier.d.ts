export declare abstract class ClassifierBase {
    abstract fit(trainX: number[][], trainY: number[]): void;
    abstract predict(testX: number[][]): number[];
    score(X: number[][], Y: number[]): number;
}

import { ClassifierBase } from '../base';
export type KernelType = 'rbf' | 'knn' | ((X: number[][], Y: number[][]) => number[][]);
export interface LabelPropagationOptions {
    kernel?: KernelType;
    gamma?: number;
    nNeighbors?: number;
    maxIter?: number;
    tol?: number;
}
export declare class LabelPropagation extends ClassifierBase {
    private kernel;
    private gamma;
    private nNeighbors;
    private maxIter;
    private tol;
    private X;
    private classes;
    private labelDistributions;
    private transduction;
    private nIter;
    constructor(options?: LabelPropagationOptions);
    private getKernel;
    fit(X: number[][], y: number[]): void;
    predict(testX: number[][]): number[];
    predictProba(testX: number[][]): number[][];
    getTransduction(): number[];
    getNIter(): number;
}

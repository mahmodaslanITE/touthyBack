import { ClassifierBase } from '../base';
import { DecisionTreeProps } from '../tree';
import { SubsetSizeOption } from '../utils/paramResolvers';
export interface RandomForestClassifierProps extends DecisionTreeProps {
    nEstimators?: number;
    bootstrap?: boolean;
    maxFeatures?: SubsetSizeOption;
    randomState?: number;
}
export declare class RandomForestClassifier extends ClassifierBase {
    private nEstimators;
    private bootstrap;
    private maxFeatures;
    private randomState?;
    private treeProps;
    private estimators;
    private fitted;
    constructor(props?: RandomForestClassifierProps);
    fit(trainX: number[][], trainY: number[]): void;
    predict(testX: number[][]): number[];
}

import { OutlierBase } from '../base/outlier';
export declare class IsolationForest extends OutlierBase {
    private max_depth;
    private iforest;
    private subsampling_size;
    private tree_num;
    private contamination;
    private random_state?;
    private rng;
    private fitted_psi;
    private offset;
    constructor(subsampling_size?: number, tree_num?: number, contamination?: 'auto' | number, random_state?: number);
    private iTree;
    private pathLength;
    private iForest;
    /**
     * average unsuccessful searches in BST (Preiss, 1999)
     * @param Psi
    */
    static AFS(Psi: number): number;
    /**
     * linearly interpolated quantile, q in [0, 1]
     */
    private static quantile;
    anomalyScore(x: number[]): number;
    fit(samplesX: number[][]): void;
    predict(samples: number[][]): (0 | 1)[];
}

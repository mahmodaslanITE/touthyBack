export interface MDSOptions {
    nComponents?: number;
    dissimilarity?: 'euclidean' | 'precomputed';
    randomState?: number;
}
export declare class MDS {
    private nComponents;
    private dissimilarity;
    private randomState?;
    private embedding;
    constructor(options?: MDSOptions);
    private static pairwiseDist;
    private static dot;
    private static matVecMul;
    private static outer;
    private static normalize;
    private static powerIteration;
    private static cloneMatrix;
    fitTransform(data: number[][]): number[][];
    getEmbedding(): number[][];
}

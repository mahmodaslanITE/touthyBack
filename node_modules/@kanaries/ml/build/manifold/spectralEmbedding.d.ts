export interface SpectralEmbeddingProps {
    nComponents?: number;
    nNeighbors?: number;
    randomState?: number;
}
export declare class SpectralEmbedding {
    private nComponents;
    private nNeighbors;
    private randomState?;
    private embedding;
    constructor(props?: SpectralEmbeddingProps);
    private static dot;
    private static matVecMul;
    private static outer;
    private static normalize;
    private static powerIteration;
    private static signFlip;
    private static cloneMatrix;
    private euclidean;
    private constructAffinity;
    fit(X: number[][]): void;
    fitTransform(X: number[][]): number[][];
    getEmbedding(): number[][];
}

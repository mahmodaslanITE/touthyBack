import { Distance } from '../metrics';
export interface RadiusNeighborsClassifierProps {
    radius?: number;
    weights?: 'uniform' | 'distance';
    metric?: Distance.IDistanceType;
    p?: number;
    outlierLabel?: number | null;
}
export declare class RadiusNeighborsClassifier {
    private radius;
    private weights;
    private metric;
    private p;
    private trainX;
    private trainY;
    private fitted;
    private outlierLabel;
    constructor(props?: RadiusNeighborsClassifierProps);
    fit(trainX: number[][], trainY: number[]): void;
    predict(testX: number[][]): number[];
}

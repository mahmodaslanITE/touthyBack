import { Distance } from '../metrics';
export declare class KDTree {
    private X;
    private leafSize;
    private root;
    private distance;
    private p;
    constructor(X?: number[][], leafSize?: number, metric?: Distance.IDistanceType, p?: number);
    fit(X: number[][]): void;
    query(X: number[][], k?: number): {
        distances: number[][];
        indices: number[][];
    };
    queryRadius(X: number[][], r: number, returnDistance?: boolean): number[][] | {
        indices: number[][];
        distances: number[][];
    };
    private _query;
    private _queryRadius;
    private pushHeap;
    private buildTree;
}

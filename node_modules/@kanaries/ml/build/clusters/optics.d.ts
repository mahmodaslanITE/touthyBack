import { ClusterBase } from '../base/cluster';
import { Distance } from '../metrics';
export interface OPTICSOptions {
    min_samples?: number;
    max_eps?: number;
    metric?: Distance.IDistanceType;
    p?: number;
    eps?: number;
}
export declare class OPTICS extends ClusterBase {
    private minSamples;
    private maxEps;
    private distance;
    private p;
    private eps;
    private reachability;
    private coreDistances;
    private ordering;
    constructor(options?: OPTICSOptions);
    fitPredict(samplesX: number[][]): number[];
    private expandClusterOrder;
    private update;
    private regionQuery;
}

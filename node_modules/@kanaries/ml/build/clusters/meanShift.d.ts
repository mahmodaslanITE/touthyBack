import { ClusterBase } from '../base/cluster';
import { Distance } from '../metrics';
export declare class MeanShift extends ClusterBase {
    private bandwidth;
    private centers;
    private max_iter;
    private distance;
    constructor(bandwidth?: number, max_iter?: number, distanceType?: Distance.IDistanceType);
    private shiftPoint;
    fitPredict(samplesX: number[][]): number[];
    getCentroids(): number[][];
}

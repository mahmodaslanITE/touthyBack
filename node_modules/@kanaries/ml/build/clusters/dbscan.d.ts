import { ClusterBase } from '../base/cluster';
import { Distance } from '../metrics';
export declare class DBScan extends ClusterBase {
    private eps;
    private minSamples;
    private distance;
    constructor(eps?: number, minSamples?: number, distanceType?: Distance.IDistanceType);
    fitPredict(samplesX: number[][]): number[];
    private expandCluster;
    private regionQuery;
}

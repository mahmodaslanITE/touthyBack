import { SubsetSizeOption } from '../utils/paramResolvers';
export interface ExtraTreeProps {
    max_depth?: number;
    min_samples_split?: number;
    criterion?: 'entropy' | 'gini';
    max_features?: SubsetSizeOption;
    randomState?: number;
}
export declare class ExtraTreeClassifier {
    private dtree;
    private max_depth;
    private feature_number;
    private min_samples_split;
    private criterion;
    private impurity;
    private max_features_prop?;
    private max_features_;
    private randomState?;
    private random;
    constructor(props?: ExtraTreeProps);
    private nodeImpurity;
    private selectFeatures;
    private attributeSelection;
    private treeGenerate;
    fit(sampleX: number[][], sampleY: number[]): void;
    private findSample;
    predict(sampleX: number[][]): number[];
}

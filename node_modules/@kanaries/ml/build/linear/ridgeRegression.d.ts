export interface RidgeRegressionProps {
    alpha?: number;
    fitIntercept?: boolean;
}
export declare class RidgeRegression {
    private alpha;
    private fitIntercept;
    private coef;
    private intercept;
    constructor(props?: RidgeRegressionProps);
    fit(X: number[][], Y: number[]): void;
    predict(X: number[][]): number[];
}

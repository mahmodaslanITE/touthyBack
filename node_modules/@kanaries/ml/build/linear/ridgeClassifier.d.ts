import { ClassifierBase } from '../base';
import { RidgeRegressionProps } from './ridgeRegression';
export interface RidgeClassifierProps extends RidgeRegressionProps {
}
export declare class RidgeClassifier extends ClassifierBase {
    private alpha;
    private fitIntercept;
    private classes;
    private models;
    constructor(props?: RidgeClassifierProps);
    fit(trainX: number[][], trainY: number[]): void;
    predict(testX: number[][]): number[];
}

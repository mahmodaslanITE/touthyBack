export interface PolynomialRegressionProps {
    degree?: number;
}
export declare class PolynomialRegression {
    private degree;
    private coef;
    private intercept;
    private fitted;
    constructor(props?: PolynomialRegressionProps);
    private transform;
    fit(X: number[][], Y: number[]): void;
    predict(X: number[][]): number[];
}

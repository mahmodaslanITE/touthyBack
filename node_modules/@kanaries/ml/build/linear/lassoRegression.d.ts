export interface LassoRegressionProps {
    alpha?: number;
    fitIntercept?: boolean;
    maxIter?: number;
    tol?: number;
}
export declare class LassoRegression {
    private alpha;
    private fitIntercept;
    private maxIter;
    private tol;
    private coef;
    private intercept;
    private fitted;
    constructor(props?: LassoRegressionProps);
    fit(X: number[][], Y: number[]): void;
    predict(X: number[][]): number[];
}

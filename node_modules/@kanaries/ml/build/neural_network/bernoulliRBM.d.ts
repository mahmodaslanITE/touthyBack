export interface BernoulliRBMOptions {
    nComponents?: number;
    learningRate?: number;
    batchSize?: number;
    nIter?: number;
    randomState?: number;
}
export declare class BernoulliRBM {
    private nComponents;
    private learningRate;
    private batchSize;
    private nIter;
    private random;
    private components;
    private interceptHidden;
    private interceptVisible;
    constructor(options?: BernoulliRBMOptions);
    private initParams;
    private hiddenProb;
    private visibleProb;
    private cdStep;
    fit(X: number[][]): void;
    partialFit(X: number[][]): void;
    transform(X: number[][]): number[][];
    fitTransform(X: number[][]): number[][];
    gibbs(V: number[][]): number[][];
}
